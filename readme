todo:

* events handling
* Space modifier functions
* prototype scene - base class for prototypes

* embedded sdf font
* textures
* ImGui
* Renderer new states (blending, sampling, ...) + batching
* support for multiple scenes
* App init parameters
* App static functions - printing OpenGL info / setting glClearColor / ...

external dependencies:

* glfw
* glm

compiles with gcc 7.2.0
