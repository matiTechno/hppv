todo:

XXX* ImGui
OOO* prototype scene - base class for prototypes

* Space modifier functions
* support for multiple scenes

* embedded sdf font
* textures
* Renderer new states (blending, sampling, ...) + batching
* App init parameters
* App static functions - printing OpenGL info / setting glClearColor / ...
* add hppv namespace
* postprocessing unit

external dependencies:

* glfw
* glm

compiles with gcc 7.2.0 on linux
