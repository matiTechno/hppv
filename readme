h p p v   -   high performance physics visualization

todo:

* setting viewport (scissor?) with space coordinates
* textures
* sdf fonts, format produced by Hiero
* Renderer new states (blending, sampling, ...) + batching
* App init parameters
* App static functions - printing OpenGL info / setting glClearColor / ...
* postprocessing
* some simple fun animations
* cmake find libraries and include paths for externals
* rendering with custom shaders

note:

* Shader is in the sh namespace, not hppv

external dependencies:

* glfw
* glm

compiles with gcc 7.2.0 on linux
