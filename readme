h p p v

todo:

* test (Renderer): replace 4x4 matrices with 3x3
* Space rotation, Camera class?
* curves, lines
* Scene::processInput() - better design
* lua bindings (see LuaTest example)
* port default imgui font
* profiling
* flat_map
* UTF-8 - input, dynamic glyph caching?
* floating point literal suffix convention (C++ / glsl)
* better cmake (finding libraries / cross-compilation)
* license
* examples (fluid, soft body, flock, text editor, game of life, SAT)

included:

* catch.hpp
* dear imgui
* glad.c glad.h
* stb_image.h
* sol2 - sol.h (LuaTest)

dependencies:

* glfw
* glm
* lua (LuaTest, optional - see cmake options)

compiles with gcc 7.2.1 on linux

runs with OpenGL 3.3,
ARB_texture_storage and ARB_base_instance extensions must be supported
