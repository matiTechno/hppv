h p p v
high performance physics visualization

todo:

* implementation & interface polishing; std algorithms / string_view / size_type
* optimize allocations (e.g. strings in Shader, replace with string_view)
* App init parameters
* better cmake (finding libraries / cross-compilation)
* flat_map
* Shader uniform setting functions
* stack allocator
* core library satellites

external dependencies:

* glfw
* glm

compiles with gcc 7.2.0 on linux
