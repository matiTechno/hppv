h p p v
high performance physics visualization

todo:

* remove App::frame
* improve Space design; rename to Rect?
* setting viewport with space coordinates
* Renderer states and batching:
                               x viewport
                               x projection
                               x shader
                                            * blending
                               x texture
                                            * sampling
                               x start
                               x count

* rendering circles
* samplers; default renderer samplers
* App init parameters
* postprocessing
* better cmake (finding libraries / cross-compilation)

note:

* Shader is in the sh namespace, not hppv

external dependencies:

* glfw
* glm

compiles with gcc 7.2.0 on linux
